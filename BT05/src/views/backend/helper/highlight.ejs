
<%
	hightLightHelper = (keyword, name) => { 
		if(keyword != '') {
			const regex = new RegExp(keyword, "gi");
			name = name.replace(regex, (search) => {
				return `<span class="hightlight">${search}</span>`
			});

			return name
		}
		return name
	}
%>